import React from 'react';
import PropTypes from 'prop-types';

const CurrentCountryStats = props => {
  return <div></div>;
};

CurrentCountryStats.propTypes = {
  props: PropTypes.object
};

export default CurrentCountryStats;
